# AI 동시 요청 보호와 향후 확장 계획

작성일: 2026-08-12

## 현재 확인된 용량

GPU AI VM은 Tesla T4 한 장에서 Gemma 12B GGUF와 CrossEncoder를 함께
실행한다. llama-server는 `-np 5`로 기동되어 있다.

동일한 영화 추천 요청을 이용한 운영 서버 측정 결과는 다음과 같다.

| 구분 | 변경 전 | 검색 최적화 후 |
|---|---:|---:|
| 단일 요청 | 약 8.8초 | 5.36초 |
| 5개 동시 요청 | 약 19.7~20.8초 | 14.4~16.6초 |

이 수치는 동시 접속자 수가 아니라 동시에 실행되는 AI 요청 수를 의미한다.
백엔드나 Kubernetes Pod만 늘려도 모든 요청이 같은 T4로 모이므로 AI 처리량은
증가하지 않는다.

## 현재 적용하는 보호 정책

- AI API 전체 실행 용량: 5
- 대기 가능한 요청: 10
- 대기 제한 시간: 40초
- 일반 AI 요청 가중치: 1
- 그룹 채팅 가중치: 5
- 대기열이 가득 찬 요청: HTTP 429와 `Retry-After: 5`
- 대기 제한 시간을 넘긴 요청: HTTP 503
- 백엔드 AI 읽기 제한 시간: 60초

그룹 채팅은 한 요청 안에서 여러 캐릭터의 LLM 생성이 발생하므로 GPU 용량
전체를 예약한다. `/health`, 영화 벡터 동기화 같은 비추론 요청은 제한 대상에서
제외한다.

AI API는 요청별 경로, HTTP 상태, 대기시간, 처리시간과 당시 실행·대기 요청 수를
`/var/log/cineverse/ai-admission.log`에 기록한다. GPU watchdog은 최근
70초를 1분마다 확인하고 다음 상황을 기존 Slack Webhook으로 알린다.

- 대기열 초과 거절 발생
- 대기 제한 시간 초과
- AI HTTP 5xx 발생
- 최대 대기시간 10초 이상
- 최대 처리시간 25초 이상

같은 상태가 계속되는 동안에는 최초 한 번만 알리고 정상화 시 복구 알림을 보낸다.

## 운영 확인 방법

```bash
sudo systemctl status cineverse-api.service
sudo systemctl status cineverse-gpu-watchdog.timer
tail -f /var/log/cineverse/ai-admission.log
tail -f /var/log/cineverse/gpu-monitor.log
```

부하 검증은 10명, 20명, 30명 순서로 수행한다. 성공률뿐 아니라 p50/p95 전체
응답시간, 최대 대기시간, HTTP 429·503·5xx, GPU 사용률과 VRAM을 함께 기록한다.

## 2026-08-12 운영 GPU 부하 테스트 결과

테스트 시각은 2026-08-12 16:57~17:00 KST다. 운영 GPU VM의 AI API
`/chat/auto`에 동일한 영화 추천 요청을 직접 전송했다. 각 단계 사이에는 10초의
정리 시간을 두었고 요청별 최대 실행시간은 70초로 제한했다. 이는 Backend와
인터넷 구간을 제외하고 GPU AI 서버 자체의 처리 용량과 admission 정책을 측정한
결과다.

| 동시 요청 | HTTP 200 | HTTP 429 | HTTP 503 | 전체 종료 | 성공 평균 | 성공 p50 | 성공 p95 | 성공 최대 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 10 | 10 | 0 | 0 | 37.857초 | 28.461초 | 27.167초 | 37.845초 | 37.845초 |
| 20 | 14 | 5 | 1 | 49.884초 | 32.096초 | 32.685초 | 49.870초 | 49.870초 |
| 30 | 12 | 15 | 3 | 44.355초 | 32.446초 | 31.699초 | 44.341초 | 44.341초 |

| 동시 요청 | GPU 최대 사용률 | 최대 VRAM | 최대 온도 |
|---:|---:|---:|---:|
| 10 | 100% | 12,634 MiB | 63°C |
| 20 | 100% | 12,634 MiB | 69°C |
| 30 | 100% | 12,634 MiB | 70°C |

결과 해석은 다음과 같다.

- 10개 요청은 모두 성공했지만 p95가 37.845초로, 현재 운영 Backend의 30초
  타임아웃을 넘는다.
- 20개 이상에서는 최대 5개 실행·10개 대기 제한이 작동해 초과 요청을 429로
  즉시 거절했다.
- 일부 대기 요청은 40초 안에 실행 슬롯을 확보하지 못해 503으로 종료됐다.
- CUDA OOM, 서비스 재시작, AI·LLM·Milvus 장애는 발생하지 않았다.
- 테스트 종료 후 AI API, llama-server, Milvus, embedder와 watchdog timer가 모두
  정상임을 확인했다.
- 이 구성은 트래픽 급증 시 서버 전체 장애를 막지만, 수십 명에게 동시에 빠른
  답변을 제공하는 처리 용량은 아니다.

원본 집계값은 `ai-load-test-20260812.json`에 함께 보관한다.

## 2026-08-17 배포 후 5명 동시 요청 검증

테스트 시각은 2026-08-17 12:09 KST다. 새 AI 파이프라인 배포 후 운영 GPU VM의
`/chat/auto`에 `액션 영화 세 편 추천해줘` 요청 5건을 동시에 전송했다. SSH
터널을 사용해 AI API를 직접 호출했으므로 Backend와 인터넷 구간의 지연은 포함하지
않는다. 요청별 제한시간은 70초로 설정했다.

| 동시 요청 | HTTP 200 | 전체 종료 | 성공 평균 | 성공 p50 | 성공 p95 | 성공 최대 |
|---:|---:|---:|---:|---:|---:|---:|
| 5 | 5 | 21.576초 | 21.038초 | 21.318초 | 21.555초 | 21.555초 |

| GPU 최대 사용률 | 최대 VRAM | 최대 온도 |
|---:|---:|---:|
| 100% | 12,634 MiB | 61°C |

5건 모두 `movie_recommend`로 분류됐고 각 응답에 영화 3편이 포함됐다. HTTP 429,
503, 5xx, 빈 답변, CUDA OOM 및 GPU Xid 오류는 발생하지 않았다. 테스트 종료 후
AI API, llama-server, Milvus, embedder와 watchdog timer가 모두 정상 상태였으며,

루트 디스크 사용률은 55%, 가용 메모리는 약 50GiB였다.

직접 AI API 기준 p95는 21.555초로 30초 이내지만 네트워크와 Backend 처리시간을
포함한 사용자 체감 시간은 이보다 길 수 있다. 또한 단일 T4의 GPU 사용률이 100%에
도달하므로, 이 결과를 수십 명 동시 사용을 보장하는 근거로 해석해서는 안 된다.

원본 집계값은 `ai-load-test-20260817.json`에 보관한다. 동일 배포의 핵심 캐릭터
대화 7건 카나리 결과는 `AI/eval/production_canary_20260817.json`에 보관한다.

## 2026-08-17 단계별 10·20·30 동시 요청 검증

테스트 시각은 2026-08-17 12:16~12:20 KST다. 앞선 5명 검증과 동일하게 운영
GPU VM의 `/chat/auto`에 `액션 영화 세 편 추천해줘` 요청을 직접 전송했다. 각
단계 사이에는 최소 10초의 정리 시간을 두었고 요청별 제한시간은 70초로 설정했다.
Backend와 인터넷 구간의 지연은 포함하지 않는다.

| 동시 요청 | HTTP 200 | HTTP 429 | HTTP 503 | 전체 종료 | 성공 평균 | 성공 p50 | 성공 p95 | 성공 최대 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 10 | 10 | 0 | 0 | 41.528초 | 30.565초 | 21.677초 | 41.520초 | 41.520초 |
| 20 | 10 | 5 | 5 | 42.540초 | 31.106초 | 22.025초 | 42.533초 | 42.533초 |
| 30 | 10 | 15 | 5 | 42.529초 | 31.108초 | 20.849초 | 42.522초 | 42.522초 |

| 동시 요청 | GPU 최대 사용률 | 최대 VRAM | 최대 온도 |
|---:|---:|---:|---:|
| 10 | 100% | 12,634 MiB | 63°C |
| 20 | 100% | 12,634 MiB | 66°C |
| 30 | 100% | 12,634 MiB | 68°C |

결과 해석은 다음과 같다.

- 10개 요청은 모두 성공했지만 직접 AI API 기준 p95가 41.520초였다.
- 20개와 30개 단계에서는 실행 5개·대기 10개 제한이 의도대로 작동했다.
- 대기열 한도를 넘은 요청은 즉시 429로 거절됐고, 실행 슬롯을 40초 안에 확보하지
  못한 요청은 503으로 종료됐다.
- 세 단계 모두 GPU 사용률이 100%에 도달했지만 CUDA OOM, GPU Xid 오류, 서비스
  재시작 및 치명적 오류는 발생하지 않았다.
- 테스트 종료 후 AI API, llama-server, Milvus, embedder와 watchdog timer가 모두
  정상이었다. 루트 디스크 사용률은 55%, 가용 메모리는 약 47GiB였다.
- 따라서 현재 단일 T4 구성은 과부하 시 서버 전체 장애는 방지하지만, 10개 이상의
  동시 AI 요청에 빠른 응답이나 전 요청 성공을 보장하지 않는다.

2026-08-12 측정과 비교하면 이번 20·30 단계의 성공 건수는 적고 503은 많았다.
반면 성공 요청의 최대 시간은 더 짧았다. 단일 실행 결과만으로 차이의 원인을 확정할
수 없으므로 성능 추세를 판단하려면 동일 조건 반복 측정이 필요하다.

원본 집계값은 `ai-load-test-20260817-staged.json`에 보관한다.

## llama-server 슬롯 프리워밍

일반 대화 단일 요청은 약 3.1~6.1초였지만, 재시작 뒤 콜드 상태에서 5건을
동시에 요청하면 각 llama.cpp 슬롯이 약 830토큰의 공통 시스템 프롬프트를
따로 처리해 전체 완료 시간이 약 14.8초까지 늘었다. 동일 슬롯의 프롬프트
캐시가 채워진 뒤에는 신규 입력 처리량이 약 111~116토큰으로 감소했고,
동일 조건의 전체 완료 시간은 약 11.5초로 줄었다.

모델·샘플링·답변 정책을 바꾸지 않고 재시작 직후 지연만 줄이기 위해 다음
도구를 사용한다.

```bash
cd /home/ubuntu/cineverse
./venv/bin/python3 ops/warm_llm_slots.py --slots 5
```

- llama-server `/health`가 준비될 때까지 최대 120초 기다린다.
- 운영의 `GENERAL_CHAT_SYSTEM_PROMPT`와 동일한 접두어로 5건을 동시에 보낸다.
- 각 요청은 폐기할 토큰 1개만 생성한다.
- 성공 JSON과 슬롯별 시간을 확인한 뒤에만 사용자 트래픽을 받는다.
- 이 작업은 콜드 슬롯 지연을 줄이는 안전장치이며, T4 한 장의 동시 생성
  처리량 자체를 늘리지는 않는다.

## 2026-08-17 단일 요청 구간 분해와 로컬 개선 후보

운영 AI API를 SSH 터널로 직접 호출해 추천 요청의 구간을 분해했다.

| 요청 | 검색·재정렬 | LLM 답변 | 전체 |
|---|---:|---:|---:|
| `액션 영화 세 편 추천해줘` | 1.495초 | 6.223초 | 7.719초 |
| `코딩과 개발자를 소재로 한 영화 추천해줘` | 1.892초 | 4.469초 | 6.368초 |

첫 번째 요청의 llama-server 측정값은 입력 456토큰, 출력 92토큰,
생성 21.12 token/s였다. 두 번째 요청은 입력 321토큰, 출력 77토큰,
생성 21.23 token/s였다. 따라서 현재 단일 추천의 주요 비용은 LLM 생성이며,
운영에서 관찰된 20초대 결과는 5건 동시 실행으로 인한 T4 자원 경합과 분리해서
해석해야 한다.

다음 변경은 로컬 회귀 테스트를 거쳐 2026-08-17 14:43~14:54 KST에 운영
AI 서버에 반영했다. 모델 파일과 llama-server 실행 옵션은 변경하지 않았다.

- LLM 호출마다 `prompt_tokens`, `output_tokens`, 프롬프트 처리시간,
  생성시간, token/s, 전체 요청시간을 `[LLMTiming]` JSON 한 줄로 기록한다.
- 비스트리밍 HTTP는 첫 토큰 도착시각을 알 수 없으므로 `ttft_seconds=null`로
  기록하고, 스트리밍 호출은 실제 첫 토큰 도착시각을 기록한다.
- 일반 추천 문장은 2~3문장, 최대 150 생성 토큰으로 제한한다.
- 일반 추천의 CrossEncoder 후보를 18개에서 12개로 줄이되, 최종
  3개 카드의 4배수를 유지한다.
- 캐릭터 답변은 전체 원문을 검증한 뒤 노출하는 현재 보호장치를 유지한다.
  검증 전 토큰을 바로 노출하면 내부 프로필·공격적 표현·역할 혼동을 되돌릴
  수 없으므로, 문장 단위 검증이 준비되기 전에는 실시간 노출로 전환하지 않는다.

로컬 자동 회귀 테스트는 266건 전체가 통과했다. 배포 후 `액션 영화 세 편
추천해줘` 요청은 HTTP 200, 영화 3편, 자연어 답변 3문장으로 정상 반환됐다.
일반 정체성 질문과 우디의 신발 밑 이름 질문도 각각 무무와 앤디로 정상
응답했다.

### 운영 반영 후 동일 조건 재측정

Backend와 인터넷 구간을 제외하고 운영 GPU VM의 AI API를 직접 호출했다.

| 구분 | 개선 전 | 개선 후 | 변화 |
|---|---:|---:|---:|
| 단일 요청 전체 | 7.719초 | 6.494초 | 15.9% 감소 |
| 5건 동시 성공 평균 | 21.038초 | 18.554초 | 11.8% 감소 |
| 5건 동시 p50 | 21.318초 | 18.688초 | 12.3% 감소 |
| 5건 동시 p95 | 21.555초 | 19.103초 | 11.4% 감소 |

5건 모두 HTTP 200, `movie_recommend`, 영화 3편, 빈 답변 없음으로 완료됐다.
단일 요청은 검색·재정렬 1.886초, LLM 답변 4.603초였다. 단일 LLM 호출은
입력 480토큰, 출력 78토큰, 생성 20.95 token/s였다. 동시 요청에서는 LLM
생성 속도가 요청별 5.27~9.33 token/s로 떨어져, 개선 후에도 단일 T4 자원
경합이 주된 확장 한계라는 점은 바뀌지 않았다.

배포 직후 AI API와 llama-server는 모두 `active`, `/health`의 LLM·Milvus·
embedder는 모두 `ok`였다. 운영 모델은
`gemma-4-12b-it-base-q4_k_m.gguf` 그대로이며 `--skip-chat-parsing`은 없다.
루트 디스크 사용률은 55%, 유휴 시 VRAM은 12,634/15,360MiB였다. 원본 결과는
`ai-load-test-20260817-latency-optimized.json`에 보관한다.

### 모호한 일반 추천의 중복 LLM 호출 제거

`오늘 뭐 볼까?`는 조건 없는 일반 추천이지만 문장 끝의 `까`가 의미 있는 검색
조건처럼 남아 검색어 재작성 LLM을 추가로 호출하고 있었다. 개선 전에는 재작성
5.795초, 검색 0.927초, 답변 4.694초로 전체 11.419초가 걸렸다.

`오늘 뭐 볼까?`, `오늘 뭐 보면 좋을까?`처럼 조건이 전혀 없는 짧은 질문은 코드
기반 일반 추천 검색어로 변환하고 재작성 LLM을 생략하도록 수정했다. 장르·분위기·
배우 등 조건이 들어간 요청은 기존 분석 경로를 유지한다.

운영 반영 후 동일 요청은 재작성 0.001초, 검색 2.027초, 답변 4.673초로 전체
6.706초에 완료됐다. 개선 전보다 4.713초, 41.3% 감소했다. HTTP 200,
`movie_recommend`, 영화 3편과 정상 자연어 답변을 확인했으며 AI API와
llama-server 상태도 모두 `active`였다.

### 조건부 CrossEncoder 정책 배포 후보

2026-08-17 요청 유형에 따른 3단계 재정렬 정책을 운영 AI API에 반영했다.
모델과 llama-server 설정은 변경하지 않았다.

- 장르·배우·감독·언어·연도·평점처럼 Milvus 필드로 검증된 조건 검색은
  CrossEncoder를 생략하고 하이브리드 RRF 순위와 저비용 품질 보정을 사용한다.
- 분류되지 않은 일반 자연어 검색은 기존처럼 상위 12개 후보만 재정렬한다.
- 분위기·자유 주제·개인화가 포함된 복합 요청은 후보를 최대 18개로 넓힌다.
- confidence 숫자 임계값은 평가 데이터가 축적되기 전까지 도입하지 않는다.

운영 Milvus 데이터를 사용한 CPU 격리 비교에서 조건 검색 3건 모두 유효한
결과를 반환했다. CrossEncoder 생략 시 장르·배우·복합 조건 검색은 각각
0.200초, 0.345초, 0.168초였고, 표준 재정렬은 11.809초, 6.606초,
7.706초였다. 이는 CPU 격리 측정이므로 운영 T4의 절대 지연시간으로 해석하면
안 된다. 상위 3개 결과의 겹침은 각각 1개, 1개, 2개였으므로 배포 후에는
카나리 요청으로 결과 관련성과 GPU 경합 감소를 함께 확인해야 한다.

운영 카나리에서는 다음 세 경로가 실제 로그로 확인됐다.

| 요청 유형 | 분기 | 후보 | 검색 | 전체 |
|---|---|---:|---:|---:|
| 액션 영화 3편 | skip | 12 | 0.273초 | 4.873초 |
| 조건 없는 일반 추천 | standard | 12 | 1.849초 | 6.399초 |
| 우울할 때 기분 전환 | complex | 18 | 1.707초 | 6.263초 |

세 요청 모두 HTTP 200, `movie_recommend`, 영화 3편과 정상 자연어 답변을
반환했다. 배포 후 AI API와 llama-server는 `active`이고 LLM·Milvus·embedder
헬스 상태는 모두 `ok`다. 기존 파일은
`/home/ubuntu/cineverse/backups/rerank-policy-20260817-canary`에 롤백용으로
보관했다.

## 추후 발전: 다중 GPU 구조

현재 프로젝트 기간에는 적용하지 않고 다음 단계의 발전 방향으로 남긴다.

```text
Backend
  -> 내부 AI Gateway / Load Balancer
       -> GPU AI VM 1
       -> GPU AI VM 2
       -> GPU AI VM N
```

확장 시 각 GPU VM은 동일한 모델·AI API 버전을 사용하고, Gateway는 활성 요청이
적은 인스턴스로 전달한다. 현재 API는 대화 이력을 요청에 포함하므로 서버 로컬
세션에 의존하지 않는 경로는 수평 확장할 수 있다. 서버 로컬 파일이나 메모리 상태에
의존하는 기능은 확장 전에 별도 저장소로 분리해야 한다.

GPU 대수는 임의로 정하지 않는다. 단일 GPU의 단계별 부하 테스트 결과에서 목표
p95 응답시간과 성공률을 정한 뒤 필요한 처리량에 맞춰 산정한다. 다중 GPU 도입 시
다음 항목을 함께 추가한다.

- GPU 인스턴스 장애 감지와 대상 자동 제외
- 모델·환경변수·벡터 인덱스 버전 일치 검증
- 롤링 배포와 최소 1대 정상 유지
- 인스턴스별 요청 수, 대기시간, 오류율 비교
- 비용 한도 및 유휴 GPU 축소 정책
- 부하 테스트 기반 용량 산정과 장애 복구 훈련
