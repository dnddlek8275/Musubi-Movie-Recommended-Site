import { useEffect, useState } from "react";
import { getAdminLogs } from "../api/adminApi";
import { EmptyState, ErrorState, LoadingState, Pagination } from "../components/AdminCommon";

export default function AdminLogPage() { const [page, setPage] = useState(1); const [result, setResult] = useState(null); const [error, setError] = useState(""); useEffect(() => { const controller = new AbortController(); getAdminLogs({ page, size: 20 }, controller.signal).then((response) => setResult(response.data)).catch((requestError) => { if (requestError.name !== "AbortError") setError(requestError.message); }); return () => controller.abort(); }, [page]); return <div className="admin-page"><section className="admin-section"><div className="admin-section-title"><div><p>AUDIT TRAIL</p><h2>관리자 활동 로그</h2></div><span>읽기 전용</span></div>{error ? <ErrorState message={error} /> : !result ? <LoadingState /> : !result.items.length ? <EmptyState /> : <div className="admin-table-wrap"><table><thead><tr><th>시간</th><th>관리자</th><th>활동</th><th>대상</th><th>설명</th><th>IP</th></tr></thead><tbody>{result.items.map((log) => <tr key={log.id}><td>{new Date(log.created_at).toLocaleString("ko-KR")}</td><td>{log.admin_name || "—"}</td><td><span className="admin-pill">{log.action}</span></td><td>{log.target_type ? `${log.target_type} ${log.target_id || ""}` : "—"}</td><td>{log.description || "—"}</td><td>{log.ip_address || "—"}</td></tr>)}</tbody></table></div>}<Pagination page={page} totalPages={result?.total_pages || 0} onChange={setPage} /></section></div>; }
